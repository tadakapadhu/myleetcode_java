//3129. Find All Possible Stable Binary Arrays// I
class Solution {
     static final int MOD = 1000000007;
    public int numberOfStableArrays(int zero, int one, int limit) {
        Integer[][][] dp = new Integer[zero+1][one+1][2];
        return (solve(zero, one, 0, limit, dp) +
                solve(zero, one, 1, limit, dp)) % MOD;
    }

    private int solve(int z, int o, int last, int limit, Integer[][][] dp) {

        if (z == 0 && o == 0)
            return 1;

        if (dp[z][o][last] != null)
            return dp[z][o][last];

        long ans = 0;

        if (last == 0) {
            for (int i = 1; i <= limit && i <= z; i++) {
                ans += solve(z - i, o, 1, limit, dp);
            }
        } else {
            for (int i = 1; i <= limit && i <= o; i++) {
                ans += solve(z, o - i, 0, limit, dp);
            }
        }

        return dp[z][o][last] = (int)(ans % MOD);
      
    }
}
