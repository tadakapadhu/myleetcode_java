class Solution {
    public int findNumbers(int[] nums) {
        int n=nums.length;
        int ecount=0;
        for(int i=0;i<n;i++){
           int count=0;
           int num=nums[i];
           while(num>0){
            num=num/10;
            count++;
            }
            if(count%2==0){
                ecount++;
            }
      }
        
        return ecount;
        
    }
}
